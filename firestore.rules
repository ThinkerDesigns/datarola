rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all reads/writes for development/testing.
    // ⚠️ FOR PRODUCTION: Replace with proper security rules that check auth.uid
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
